import React, { useContext, useEffect } from 'react';
import { ClockContext } from '../contexts/ClockContext';

const Display = () => {
  const {} = useContext(ClockContext);

  useEffect(()=>{
    
  })

  function _tick_clock(tick_rate) {
    
  }

  return (
    <h1>Display</h1>
  );
}

export default Display;